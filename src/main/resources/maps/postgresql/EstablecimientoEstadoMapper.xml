<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="cl.mineduc.came.apoyo_mejora_continua.mappers.EstablecimientoEstadoMapper">
	<resultMap id="BaseResultMap"
		type="cl.mineduc.came.apoyo_mejora_continua.modelo.EstablecimientoEstado">
		<id column="id_establecimiento_estado"
			property="idEstablecimientoEstado" />
	</resultMap>

	<sql id="query">
		select _est.id_establecimiento_estado
		from came.came_establecimiento_estado _est
	</sql>

	<delete id="deleteById" parameterType="java.lang.Long">
		delete from
		came.came_establecimiento_estado
		where id_establecimiento_estado =
		#{id_establecimiento_estado,jdbcType=BIGINT}
	</delete>

	<insert id="insert"
		parameterType="cl.mineduc.came.apoyo_mejora_continua.modelo.EstablecimientoEstado">
		insert into came.came_establecimiento_estado
		(id_establecimiento_estado)
		values
		(#{id_establecimiento_estado,jdbcType=BIGINT})
	</insert>

	<select id="getAll" resultMap="BaseResultMap">
	<include refid="query"></include>
		order by _est.id_establecimiento_estado asc
	</select>
</mapper>