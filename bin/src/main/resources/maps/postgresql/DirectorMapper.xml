<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="cl.mineduc.came.apoyo_mejora_continua.mappers.DirectorMapper">
	<resultMap id="BaseResultMap"
		type="cl.mineduc.came.apoyo_mejora_continua.modelo.Director">
		<id column="id_director" property="idDirector" />
	</resultMap>
	
	<sql id="query">
		select _d.id_director
		from came.came_director _d
	</sql>	
	
	<delete id="deleteById" parameterType="java.lang.Long">
		delete from came.came_director
		where id_director = #{idDirector,jdbcType=BIGINT}
	</delete>
		
	<insert id="insert"
		parameterType="cl.mineduc.came.apoyo_mejora_continua.modelo.Director">
		insert into came.came_director (id_director)
		values (#{idDirector,jdbcType=BIGINT})
	</insert>
	
	<select id="getAll" resultMap="BaseResultMap">
		<include refid="query"></include>
		order by _d.id_director asc
	</select>
</mapper>